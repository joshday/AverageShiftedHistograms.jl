module BivariateTest

using AverageShiftedHistograms, FactCheck
a = AverageShiftedHistograms

facts("Bivariate") do
    context("Bin2") do
    end

    context("Ash2") do
    end

    context("methods") do
    end
end

end # module
